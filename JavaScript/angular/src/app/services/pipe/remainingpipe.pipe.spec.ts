import { RemainingpipePipe } from './remainingpipe.pipe';

describe('RemainingpipePipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingpipePipe();
    expect(pipe).toBeTruthy();
  });
});
