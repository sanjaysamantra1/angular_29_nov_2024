import { Routes } from '@angular/router';
// import { LoginComponent } from './compoents/login/login.component'; // Ensure correct path
// import { SignupComponent } from './compoents/signup/signup.component'; // Ensure correct path

export const routes: Routes = [
//   { path: '', redirectTo: 'login', pathMatch: 'full' }, // Default route to Login
//   { path: 'login', component: LoginComponent }, // Login route
//   { path: 'signup', component: SignupComponent }, // Signup route
//   { path: '**', redirectTo: 'login' }, // Wildcard route to handle invalid paths
];
