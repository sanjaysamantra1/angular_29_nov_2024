import { SearchMembersPipe } from './search-members.pipe';

describe('SearchMembersPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMembersPipe();
    expect(pipe).toBeTruthy();
  });
});
