var arr1 = [10, 20, 30, 40, 50];

var arr2 = new Array(5); // treats as length 5 array, no data is present.

var arr3 = new Array(1, 3, 4, 5, 6); // treats as array items.

console.log(`${arr1} - ${arr2} - ${arr3}`);

// ITERATING OVER AN ARRAY:
// for loop
// for in
// for of
// forEach();

// properties: length, delete

