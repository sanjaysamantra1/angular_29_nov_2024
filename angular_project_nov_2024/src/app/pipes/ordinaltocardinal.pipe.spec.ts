import { OrdinaltocardinalPipe } from './ordinaltocardinal.pipe';

describe('OrdinaltocardinalPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdinaltocardinalPipe();
    expect(pipe).toBeTruthy();
  });
});
